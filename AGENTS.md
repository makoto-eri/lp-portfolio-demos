# AGENTS.md — LPポートフォリオデモ集

GitHub Pagesで公開する架空・非公式の静的LPデモ集です。閲覧者が実在店舗、公式サイト、実サービスと誤認しないことを最優先にします。

## 構成と正本

- ルート `index.html` はデモ一覧、各デモフォルダの `index.html` が公開ページの正本。
- 共通挙動は `shared/demo-banner.js`、一覧画像は `assets/previews/`。`output/` は派生成果物であり編集元にしない。
- `index-previous-*` など履歴用ファイルと既存の未コミット変更を、明示依頼なしで削除・復元しない。
- 静的HTML/CSS/JSを維持し、必要のないフレームワーク、build環境、DB、認証、外部APIを追加しない。

## 公開デモの必須条件

- すべての公開ページに `<meta name="robots" content="noindex, nofollow">` と、画面上で分かる「架空・非公式デモ」表示を置く。
- フォーム、予約、電話、LINE、購入CTAは送信・保存しない。分析タグ、外部通信、個人情報収集を明示要件なしで追加しない。
- 店名、人物、住所、料金、口コミ、実績、効果、資格は架空であることを明確にし、実在情報や成果数値を創作しない。
- 画像は出典、利用許諾、AI生成区分を確認する。参考ライブラリから画像をコピーせず、公開ソースには最適化した許諾済みローカル素材を使う。
- 新規Base64埋め込みを公開ソースへ追加しない。単一HTML＋Base64版が必要な場合だけ `output/` に派生生成する。
- ヒーローの見出し、CTA、価格、ロゴは画像へ焼き込まず、HTML/CSSと独立画像で構成する。
- 例外（2026-08-09 追加・web-design-studio 画像LPルートのデモのみ）: ゾーンAの画像に限り、屋号ロゴと装飾的な短い見出し（15字以内）の焼き込みを認める。**CTA・価格・日付・限定訴求・規制文言は例外なく焼き込み禁止**で、%座標のHTMLオーバーレイに置く。焼き込んだ文言は必ず `alt` に全文を入れ、生成プロンプトを案件フォルダに保管する。該当デモ: `tsukuyomi/`・`inahodo/`・`tsumiki/`。この例外を使わないデモは上の行のとおり焼き込み一切禁止。

## デモ追加時

- 新しいデモフォルダと `index.html`、プレビュー画像、ルート一覧のカードと件数、`README.md` を同じ変更内で同期する。
- 相対リンク、アンカー、画像パス、デモバナー、robots、CTA非送信を確認する。
- デプロイ、push、公開URL変更は明示依頼がある場合だけ行う。

## 完了確認

- ローカルHTTPサーバーでルートと変更したデモを開く。
- 320 / 390 / 768 / 1280 / 1440pxで横スクロール、固定CTA、画像、キーボード操作、フォーカス、コンソールエラーを確認する。
- `prefers-reduced-motion`、見出し構造、alt、コントラスト、外部通信の有無を確認し、未確認事項を報告する。
